// src/store/peopleReviewViewStore.ts
// "Grid or cards" preference for a review session's employee roster
// (see createViewStore).
import { createViewStore } from './createViewStore';

export const usePeopleReviewViewStore = createViewStore('people_review_view');

// src/store/planningViewStore.ts
// "Grid or cards" preference for the plan-sessions list (see createViewStore).
import { createViewStore } from './createViewStore';

export const usePlanningViewStore = createViewStore('planning_view');

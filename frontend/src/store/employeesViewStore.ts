// src/store/employeesViewStore.ts
// "Grid or cards" preference for the employees list (see createViewStore).
import { createViewStore } from './createViewStore';

export const useEmployeesViewStore = createViewStore('employees_view');

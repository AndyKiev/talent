// src/store/recruitmentViewStore.ts
// "Grid or cards" preference for the recruitment tasks list (see createViewStore).
import { createViewStore } from './createViewStore';

export const useRecruitmentViewStore = createViewStore('recruitment_view');

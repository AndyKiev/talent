// src/store/employeeEventsViewStore.ts
// "Grid or cards" preference for an employee's events list (see createViewStore).
import { createViewStore } from './createViewStore';

export const useEmployeeEventsViewStore = createViewStore('employee_events_view');
